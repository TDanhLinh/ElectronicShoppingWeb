import {createContext} from "react";

const DataTableContext = createContext(undefined);
const LableTableContext = createContext(undefined);
const ElementTableContext = createContext(undefined);

export {DataTableContext, LableTableContext, ElementTableContext};